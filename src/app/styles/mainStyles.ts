import { ScaledSheet } from 'react-native-size-matters';

export const mainStyles = ScaledSheet.create({
container: {
  flex: 1,
  backgroundColor: '#fff',
  padding: '16@s',
},
  safeArea: {
    backgroundColor: '#fff',
    flex:1,
  },
});
